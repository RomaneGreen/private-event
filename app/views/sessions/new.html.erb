<%= form_for(:sessions, url:new_session_path) do |f| %>

     <%= f.label :name %>
     <%= f.text_field :name, class: 'form-control' %> <br>

     <%= f.submit "Log in", class: "btn btn-primary" %>
   <% end %>
